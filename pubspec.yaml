name: superdeck_workspace
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  melos: ^6.3.3
  mix: ^2.0.0-rc.0

dev_dependencies:
  mix_generator: ^2.0.0-rc.0

lint_staged:
  '**.dart': dart fix --apply && dcm fix
