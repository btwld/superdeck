name: superdeck_workspace
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.1"

dependencies:
  melos: ^6.3.3
  mix: ^2.0.0-rc.0

lint_staged:
  '**.dart': dart fix --apply && dcm fix
