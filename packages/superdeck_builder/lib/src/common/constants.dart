/// Constants used throughout the builder package
class BuilderConstants {
  // Timeouts
  static const defaultTimeoutSeconds = 30;
  static const browserTimeoutSeconds = 60;
  
  // File extensions
  static const markdownExtension = '.md';
  static const jsonExtension = '.json';
  
  // Other constants
  static const defaultLineLength = 80;
}
