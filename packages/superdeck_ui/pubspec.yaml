name: superdeck_ui
description: A UI components library for SuperDeck with mix and remix support.
version: 0.0.1
homepage: https://github.com/leoafarias/superdeck

publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  mix: ^2.0.0-dev.4
  remix: ^0.1.0-beta.1
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  dart_code_metrics_presets: ^2.19.0

flutter:
