/// The executable name for the deploy tool.
const deployToolName = 'superdeck-deploy';

/// The current version of the deploy tool.
const deployToolVersion = '1.0.0';

/// Human-readable description shown in `--help` output.
const deployToolDescription =
    'Deployment tooling for SuperDeck presentations '
    '(GitHub Pages, Firebase Hosting).';
