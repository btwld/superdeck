## 1.0.0

- Initial release of `superdeck_deploy`.
- `superdeck-deploy github-pages`: publish a built SuperDeck web app to GitHub Pages via an isolated git worktree.
- `superdeck-deploy firebase`: deploy a built web app to Firebase Hosting.
