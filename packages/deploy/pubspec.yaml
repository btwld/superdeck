name: superdeck_deploy
description: Deployment tooling for SuperDeck presentations (GitHub Pages).
version: 1.0.0
homepage: https://github.com/btwld/superdeck
documentation: https://docs.page/btwld/superdeck

executables:
  superdeck-deploy: main

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  args: ^2.6.0
  path: ^1.9.0
  mason_logger: ^0.3.1

dev_dependencies:
  dart_code_metrics_presets: ^2.19.0
  lints: ^5.0.0
  test: ^1.25.8
