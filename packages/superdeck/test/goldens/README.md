These golden tests pin a small set of stable slide-rendering surfaces at the deck's fixed `kResolution`; when an intentional visual change updates these baselines, regenerate them from the repository root with `fvm flutter test packages/superdeck/test/goldens/slide_goldens_test.dart --update-goldens` and review the resulting PNG diffs before committing.
